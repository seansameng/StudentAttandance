<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for dark mode. -->
    <style name="Theme.StudentAttendance.Night" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorPrimaryVariant">@color/primary_color</item>
        <item name="colorOnPrimary">@color/black</item>
        
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_light</item>
        <item name="colorSecondaryVariant">@color/secondary_color</item>
        <item name="colorOnSecondary">@color/black</item>
        
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
        
        <!-- Background colors. -->
        <item name="android:colorBackground">@color/black</item>
        <item name="colorSurface">@color/black</item>
        <item name="colorOnSurface">@color/white</item>
        
        <!-- Text colors. -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white_70</item>
        <item name="android:textColorHint">@color/white_50</item>
        
        <!-- Card colors. -->
        <item name="colorSurfaceVariant">@color/black_70</item>
        <item name="colorOnSurfaceVariant">@color/white_70</item>
        
        <!-- Error colors. -->
        <item name="colorError">@color/error_light</item>
        <item name="colorOnError">@color/black</item>
        
        <!-- Success colors. -->
        <item name="colorTertiary">@color/success_light</item>
        <item name="colorOnTertiary">@color/black</item>
        
        <!-- Custom attributes -->
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Theme without action bar for full-screen activities -->
    <style name="Theme.StudentAttendance.NoActionBar" parent="Theme.StudentAttendance.Night">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>