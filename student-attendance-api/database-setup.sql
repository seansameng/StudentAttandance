-- Database Setup Script for Student Attendance System
-- Run this script in MySQL to create the database

-- Create the database (if it doesn't exist)
CREATE DATABASE IF NOT EXISTS attendance_system;

-- Use the database
USE attendance_system;

-- Show confirmation
SELECT 'Database "attendance_system" is ready!' AS Status;

-- Optional: Show all databases to verify
SHOW DATABASES;
